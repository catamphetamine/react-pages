import { render_before_content, render_after_content } from '../server/html'
import render from '../server/render'

/**
 * Generates `index.html` for non-server-side-rendered static websites.
 * @return {string}
 */
export default function generate_entry_html()
{
	throw new Error('Will be implemented')
}